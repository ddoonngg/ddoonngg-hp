<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Append API" stopProcessing="true">
          <match url="^(.*)" />
          <action type="Redirect" url="/api/{R:1}" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
