{
  "name": "ddoonngg-hp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:client": "cd client && npm run && npm run build",
    "start:server": "cd server && npm run && npm run tsc",
    "build": "npm run build:client && echo 'Frontend built successfully!' && npm run start:server && echo 'Backend built successfully!'",
    "start": "node server/dist/server.js",
    "deploy": "npm run build && NODE_ENV=production npm run start && echo 'Deployment completed!'"
  }
}
