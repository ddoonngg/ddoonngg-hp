{
  "name": "ddoonngg-hp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:client": "cd client && yarn && yarn build",
    "start:server": "cd server && yarn && yarn tsc",
    "build": "set NODE_ENV=production && yarn build:client && echo 'Frontend built successfully!' && yarn start:server && echo 'Backend built successfully!'",
    "start": "node server/dist/server.js",
    "deploy": "yarn build && yarn start && echo 'Deployment completed!'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
